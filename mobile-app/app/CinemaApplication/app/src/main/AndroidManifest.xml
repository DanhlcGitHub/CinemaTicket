<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.luulac.cinemaapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.FilmActivity"/>
        <activity android:name=".activities.OrderTicketActivity"/>
        <activity android:name=".activities.ChoiceSeatsActivity"/>
        <activity android:name=".activities.PaymentApplicationActivity"/>
        <activity android:name=".activities.FinishPaymentActivity"/>
        <activity android:name=".activities.PaypalPaymentActivity"/>
        <activity android:name=".activities.LoginActivity"/>
        <activity android:name=".activities.OrderDetailActivity"/>
        <activity android:name=".activities.ResellTicketActivity"/>
        <activity android:name=".activities.FilmInfomationActivity"/>
        <activity android:name=".activities.CinemaActivity"/>
        <activity android:name=".activities.RegisterAccountActivity"/>

        <service android:name=".services.ChangeStatusService" android:stopWithTask="false" />
    </application>

</manifest>